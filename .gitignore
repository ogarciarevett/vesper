# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Rust / Tauri build artifacts (packages/vesper-desktop — the native shell, DEV-112).
# Cargo.lock IS committed (this is an application binary — reproducible builds).
target/
/packages/vesper-desktop/src-tauri/gen/
# generated daemon-embed assets + the compiled sidecar binary (DEV-112 Slice 2)
/packages/vesper-cli/src/generated/
/packages/vesper-desktop/src-tauri/binaries/

# Vesper local runtime directory
.vesper/

# impeccable (UI/UX audit skill) runtime artifacts — critique snapshots, live-mode config
.impeccable/

# Environment / secrets
.env
.env.*
!.env.example

# Logs
*.log

# OS
.DS_Store

# local, per-dev agent working memory (seeded from .ai/memory.example.md via `bun run sync:ai`)
.ai/memory.md

# design specs are LOCAL working drafts only — never distributed via the public repo.
# the committed record of a cycle is the commit message + cycle-log.md (see .ai/context.md).
/specs/

# generated per-tool agent assets — sources live in .ai/; run `bun run sync:ai`
/.claude/commands/
/.claude/agents/
/.claude/skills/
/.opencode/commands/
/.opencode/agents/
/.gemini/commands/
/.gemini/agents/
/.gemini/skills/

# per-user local Claude Code settings
.claude/settings.local.json

# local-only demo recorder + its intermediates (not committed)
/scripts/record-demo.sh
/docs/imgs/demo.cast
/docs/imgs/demo.mov
# docs/imgs/demo.gif IS committed (embedded in the README); the .cast is also deleted after rendering
