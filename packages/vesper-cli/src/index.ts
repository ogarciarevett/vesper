#!/usr/bin/env bun
// @vesper/cli — `vesper` operator command.
// Foundation verbs (init, vault, cli, hello, status, daemon) land here through the feature loop.
// Placeholder entry keeps the bin resolvable while scaffolding.
export {};
