# MEMORY — Vesper (local agent working log)

Per-developer, **gitignored** working log shared across the AI CLIs on your machine
(Claude Code, opencode, Codex, Gemini). Seeded from this template by `bun run sync:ai`.
Protocol: see the "Memory protocol" section of `.ai/pipeline.md`.

Terse entries only. Durable decisions → commit message / Linear / `cycle-log.md` / `docs/adr/`,
NOT here. This is a working scratchpad, separate from claude-mem (machine-level cross-session
memory) and from the committed `cycle-log.md`.

**NEVER write secrets here** — no Keychain values, GitHub tokens, Discord webhooks, or API keys.

## Log
